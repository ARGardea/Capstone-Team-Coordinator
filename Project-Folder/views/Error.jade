extends BaseLayout

block content
    link(href='/styles/Error.css', rel='stylesheet')
    div(class="main")
        h2
            |ERROR
        - if (typeof(message) !== 'undefined'){
            | #{message}
        -}else{
            | Error message not found
        -}